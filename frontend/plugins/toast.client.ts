/**
 * Toast system initialization plugin
 * Sets up the global toast notification system
 */
export default defineNuxtPlugin(() => {
  // This plugin will be used to initialize the toast system
  // The actual initialization happens in the app.vue file
  // where the ToastContainer component is mounted
});
